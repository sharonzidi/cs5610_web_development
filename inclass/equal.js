(function() {

    // true, because JavaScript will convert the types
    console.log('1 == "1"');
    console.log(1 == "1");

    // false, because the types are different
    console.log('1 === "1"');
    console.log(1 === "1");

    // question, why do you think JavaScript has this so prominently?


})();